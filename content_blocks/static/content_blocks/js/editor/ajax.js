function ajaxForm(form, callback) {
  // Take a form and submit it via ajax.
  // Run the callback on success.
}

function ajaxButton(button, callback) {
  // Take a button and submit it via ajax.
  // Run the callback on success.
}

function ajaxSetup() {
  // Probably doesn't need to be a function can just go to top of file.
  // Get csrf from cookie, provide some default options for ajax calls.
}
